# Use the REST API instead of the local mock adapters.
VITE_API_MODE=real

# Empty during local development: Vite forwards /api to the target below.
VITE_API_BASE_URL=
VITE_BACKEND_PROXY_TARGET=http://localhost:8080
